name: sal
channels:
  - pytorch
  - nvidia
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - git
  - libsndfile
  - pip:
    # conda env create -f cluster/env.yml
    - -r requirements.txt
