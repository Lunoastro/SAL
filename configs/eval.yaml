# @package _global_

defaults:
  - _self_
  - data: partialspoof
  - model: large_cf_8labels_2loss
  - logger: tensorboard
  - trainer: gpu
  - paths: default
  - extras: default
  - hydra: default

task_name: eval
tags:
  - eval
clean_tag: ${join:"-", ${tags}}

# passing checkpoint path is necessary for evaluation
ckpt_path: ${oc.env:SAL_EVAL_CHECKPOINT}

data:
  batch_size: 1
  num_workers: 4